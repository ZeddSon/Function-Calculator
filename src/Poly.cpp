#include "Poly.h"

Poly::Poly():Function("0"){}

Poly::Poly(const std::vector<float>& coefficient): m_coefficient(coefficient)
{
	std::string title;
	for (auto i = coefficient.end(); i > coefficient.begin(); i--) {
		if(i >= 0)
	}
}

